#include<stdio.h>

int main()

{
	int a,b,flag=0;
	printf("Enter the number a:");
	scanf("%d",&a);
	for(a=1;a<=b;a++)
	{
		if(b%a==0);
		{
			flag++;
		}
		if(flag==1)
		{
			printf("Prime Number");
		}
		else
		{
			printf("Not a Primn Number");
		}
	}
	
	return 0;
}
